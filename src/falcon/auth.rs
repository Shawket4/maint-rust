//! Reserved for any login/logout passthrough needs.
//! The architecture says the desktop app calls Falcon's /api/login directly (§3.1),
//! so maint-rust does not currently proxy auth. Module kept to match §10 layout.
